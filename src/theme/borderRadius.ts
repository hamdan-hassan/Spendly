/**
 * Spendly Design System — Border Radius
 *
 * Consistent rounding scale for UI elements.
 */

export const borderRadius = {
  /** 4px — Subtle rounding (tags, badges) */
  xs: 4,
  /** 8px — Inputs, small cards */
  sm: 8,
  /** 12px — Standard cards */
  md: 12,
  /** 16px — Featured cards */
  lg: 16,
  /** 20px — Modal cards */
  xl: 20,
  /** 24px — Large containers */
  xxl: 24,
  /** 9999px — Pills, fully rounded */
  full: 9999,
} as const;
